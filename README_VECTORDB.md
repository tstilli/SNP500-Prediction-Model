These will be needed to access the S3 bucket with the SEC documents
export AWS_ACCESS_KEY_ID='ask taaj'
export AWS_SECRET_ACCESS_KEY='ask taaj'
export AWS_DEFAULT_REGION='us-east-1'

to populate database with documents run embedder.py
-   cd models
-   python embedder.py

